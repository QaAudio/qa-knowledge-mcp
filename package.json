{
  "name": "@quantumaudio/knowledge-mcp",
  "version": "0.1.0",
  "description": "MCP server for semantic search over QuantumAudio documentation (Qdrant)",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=24.14.1"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/tools.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^4.4.3",
    "@quantumaudio/qa-knowledge": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.9.3"
  },
  "license": "Apache-2.0"
}
